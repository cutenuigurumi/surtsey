<%= form_for(@case_category) do |f| %>
  <% if @case_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@case_category.errors.count, "error") %> prohibited this case_category from being saved:</h2>

      <ul>
      <% @case_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
